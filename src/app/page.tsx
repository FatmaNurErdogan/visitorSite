// The site's home page, opens when you just type the domain in the address bar.
// Empty for now — later we might redirect from here to login etc.
export default function Home() {
  return (
    <main className="home-page">
      <h1>Visitor Management System</h1>
      <p>Skeleton is ready — pages will be filled in as we go.</p>
    </main>
  );
}
