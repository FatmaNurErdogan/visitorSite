BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [tys].[vms_meeting_room] ADD [perks] NVARCHAR(500);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
